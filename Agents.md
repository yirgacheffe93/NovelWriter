# 项目概述
这是一个小说续写项目

# 技术栈
- 前端：React 18, Tailwind CSS, TypeScript
- 后端：Python 3.10, FastAPI, SQLAlchemy
- 数据库：PostgreSQL

# 常用命令
- conda环境: novel


---

# AI 行为准则 (Karpathy 守则)
请在协助编写和修改代码时，严格遵守以下 4 条核心原则。这代表了该项目的最高优先级行事风格：

## 1. 编码前先思考 (Think Before Coding)
不要盲目假设，不要隐藏困惑，主动展示方案的权衡。在开始实现之前：
- 明确说明你的假设条件。
- 如果需求有歧义或不确定，**停下来提问**，不要默默替我做决定。
- 如果有多种实现方式，列出它们并说明利弊。
- 如果有更简单的替代方案，请直接指出来。

## 2. 简单优先 (Simplicity First)
用最少的代码解决当前问题。严禁投机性功能：
- **不加没被要求的功能**。
- **不为单次使用的代码做过度抽象**。
- **不加没被要求的“灵活性”和“配置项”**。
- 不要为现实中不可能发生的场景编写冗长的错误处理。
- 如果 200 行代码可以精简为 50 行，请选择 50 行的版本。问问自己：“资深工程师会觉得这太复杂了吗？”

## 3. 外科手术式修改 (Surgical Changes)
只改必须改的地方，只清理你自己弄乱的代码。在编辑现有代码时：
- **严禁“顺手优化”相邻的代码、注释或格式**。
- 不要重构没有坏掉的东西。
- 严格匹配当前文件的代码风格（空格、缩进、命名），即使你个人更喜欢另一种风格。
- 如果你发现无关的死代码或 bug，用文字提醒我，但**不要擅自删除或修改它**。
- 检验标准：你修改的每一行代码，都必须能直接追溯到我当次的具体要求。

## 4. 目标驱动执行 (Goal-Driven Execution)
定义成功标准，闭环验证。将模糊的任务转化为可验证的步骤：
- “添加验证” → “先写无效输入的测试用例，再让其通过”
- “修复 Bug” → “先写能复现 Bug 的测试，再修复它”
- “重构模块 X” → “确保重构前后测试都能通过”
- 对于多步任务，请先列出简要计划，每做完一步进行一次检查验证。

## 5. Python 编码与注释规范 (Code Style & Documentation)

在生成和修改 Python 代码时，必须严格遵守以下规范：

### 5.1 代码组织与文件结构 (Code Organization)
- **分离关注点**：严禁将路由层、核心业务逻辑（如大模型调用、状态机流转）和底层数据访问（如 Elasticsearch/Milvus 操作）柔和在同一个长函数中。
- **导入规范**：严格遵循 PEP 8 的 Import 顺序，分为三块并用空行隔开：
  1. Python 标准库 (如 `os`, `typing`)
  2. 第三方依赖 (如 `pydantic`, `pymilvus`)
  3. 本地模块绝对导入

### 5.2 强制类型提示 (Mandatory Type Hints)
- 所有函数的**参数**和**返回值**必须包含极其严格的类型提示（Type Hints）。
- 遇到复杂嵌套数据结构，优先使用 `Pydantic` 模型或 `TypedDict` 进行约束，不要使用模糊的 `Dict[str, Any]`。

### 5.3 文档字符串 (Docstrings)
- **模块级（文件）注释**：每个新建的 `.py` 文件顶部必须有一句话，说明该模块在系统中的核心职责。
- **函数/类级注释**：强制使用 **Google Style** Docstrings。必须清晰列出 `Args:`（参数）、`Returns:`（返回值）和可能抛出的 `Raises:`（异常）。
- **禁止废话**：如果函数名和类型提示已经完全自解释（如 `def get_user_by_id(user_id: int) -> User:`），则可以省略 Docstring，保持代码紧凑。

### 5.4 行级注释原则 (Line-level Comments)
- **只解释 Why，不解释 What**。
- **严禁过度注释**：不要为基础的 API 调用、循环和显而易见的逻辑添加注释。
- **记录妥协**：如果为了绕过某个特定的 Bug 或性能瓶颈使用了非标准写法（例如特殊的 PySpark 算子调优），必须在代码上方用注释详细说明原因。

### 5.5 命名规范 (Naming Conventions)
在新建文件、类和变量时，必须严格遵守 PEP 8 命名规范，并保持极高的业务语义清晰度：
- **文件与包名 (Files & Packages)**：必须使用全小写字母和下划线 `snake_case`（例如：`milvus_client.py`, `rag_pipeline.py`）。严禁使用中划线或驼峰命名。
- **工作流与顺序性命名 (Pipeline & Sequential Naming)**：对于属于同一处理链路、具有执行先后顺序或强逻辑关联的文件，必须采用 **[同一前缀]_[顺序标识]_[动作描述]** 的格式，以确保文件树自然排序。
  - ✅ 正确示例：`data_a_process.py`, `data_b_format.py`, `data_c_embed.py`。
- **类名 (Classes)**：必须使用大驼峰命名法 `PascalCase`（例如：`TenantManager`, `DocumentRetriever`）。
- **函数与变量名 (Functions & Variables)**：必须使用全小写字母和下划线 `snake_case`（例如：`extract_context()`, `tenant_id`）。
- **常量 (Constants)**：全局配置或状态常量必须使用全大写字母和下划线 `UPPER_SNAKE_CASE`。
- **杜绝“垃圾桶”命名**：严禁使用 `utils.py`、`helper.py` 等毫无业务上下文的通用文件名。工具类必须指明具体用途（例如：`text_cleaner.py`）。
- **测试文件**：单元测试文件必须以 `test_` 开头，并与其测试的目标模块严格对应。

## 6. 资源防御与内存安全 (Resource Defense & Memory Safety)
在处理大规模数据集、构建知识库或读取大文件时，必须采取防御性编程策略，严禁写出可能打爆内存的代码：
- 拒绝全量加载：严禁对大文件使用 .read()、对大型数据库查询使用 .fetchall()。如果无法确定文件大小，默认将其视为超大文件处理。
- 强制分块与流式处理 (Chunking & Streaming)：必须优先使用生成器 (yield)、按行读取 (for line in file:)、分页查询 (Pagination/Cursor) 或设置合理的 chunk_size。
- 管道化数据流转：在将数据写入 Elasticsearch 或 Milvus 等存储节点，或使用 PySpark 等框架进行批量转换时，确保数据是“流经”内存（Lazy Evaluation），而不是在单机节点或单个长函数中全量堆积。
- 生命周期管理：强制使用上下文管理器 (with 语句) 确保文件句柄和网络连接的及时释放。对于占用极大的中间变量，在作用域结束时要有意识地让其失效或被回收。

## 7. 进度与结果展示(Process & Result Display)
- 在处理数据的时候，应该是tqdm展示数据处理的进度
- 在展示数据结果的时候，应该是prettytable
